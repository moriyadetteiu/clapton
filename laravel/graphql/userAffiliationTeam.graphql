type UserAffiliationTeam {
    id: ID!
    user: User @belongsTo
    team: Team @belongsTo
}

extend type Mutation @guard {
    userJoinsTeam(user_id: ID!, team_id: ID!): UserAffiliationTeam
        @field(resolver: "App\\GraphQL\\Mutations\\UserJoinsTeam")
}
