mutation CreateEventMutation($input: EventInput!) {
  createEvent(input: $input) {
    id
    name
    event_dates {
      id
      event_id
      name
      date
      is_production_day
      created_at
      updated_at
    }
    created_at
    updated_at
  }
}
