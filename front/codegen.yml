overwrite: true
schema: "http://nginx/graphql"
documents: "./apollo/**/*.gql"
generates:
  ./apollo/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-document-nodes"
